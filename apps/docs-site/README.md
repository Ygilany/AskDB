# AskDB docs site

Astro/Starlight static site. Source lives in `src/content/docs/`.

```bash
# from repo root
pnpm docs:dev      # dev server at http://localhost:4321
pnpm docs:build    # production build → apps/docs-site/dist/
pnpm docs:preview  # preview the production build locally
```
