---
title: "AskDB"
description: "AskDB is the open-source Text-to-SQL toolkit your data team trusts. Turn natural-language questions into reviewed SQL your app runs — your schema, your model, your database. Nothing leaves your stack."
template: "splash"
hero:
  title: '<span class="home-hero-title">The open-source <span class="home-hero-accent">Text-to-SQL toolkit</span><br/>your data team trusts.</span>'
  tagline: 'Ask your data. Keep control. AskDB turns natural-language questions into reviewed SQL your app runs &mdash; your schema, your model, your database. The toolkit handles the SQL so your team can focus on the business context, not the plumbing.'
  actions:
    - text: "Quickstart"
      link: "/quickstart/"
      variant: "primary"
      icon: "right-arrow"
    - text: "How it works"
      link: "/concepts/how-askdb-works/"
      variant: "secondary"
      icon: "open-book"
---

import HomeCliStrip from "../../components/HomeCliStrip.astro";
import HomeDifferentiators from "../../components/HomeDifferentiators.astro";
import HomeWorkflow from "../../components/HomeWorkflow.astro";
import HomeEngines from "../../components/HomeEngines.astro";
import HomePrivacyBoundary from "../../components/HomePrivacyBoundary.astro";
import { Tabs, TabItem } from "@astrojs/starlight/components";

<div class="home-landing">

<HomeCliStrip />

<div class="home-pill-row" role="list">
  <span class="home-pill" role="listitem">Postgres · MySQL · SQLite · SQL Server</span>
  <span class="home-pill" role="listitem">Open source · Apache 2.0</span>
  <span class="home-pill" role="listitem">No data leaves your stack</span>
</div>

## What makes AskDB different

<HomeDifferentiators />

## How it works

<HomeWorkflow />

## Embed it in your app

<p class="home-terminal-intro">A few lines of TypeScript: load a schema, ask a question, run the SQL through your own connection pool.</p>

<Tabs syncKey="wiring">
  <TabItem label="Direct (Vercel AI SDK)">

```ts
import { ask, loadSchema } from "@askdb/core";
import { openai } from "@ai-sdk/openai";
import { Pool } from "pg";

const schema = await loadSchema("./my-app.schema");
const pool = new Pool({ connectionString: process.env.DATABASE_URL });

const { sql } = await ask({
  question: "Which customers signed up last week?",
  schema,
  dialect: "postgres",
  model: openai("gpt-4o-mini"),
});

// Log or approve `sql` here, then run it through your own pool.
const result = await pool.query(sql);
```

  </TabItem>
  <TabItem label="Config-driven (@askdb/ai)">

```ts
import { createAskDb } from "@askdb/client";
import { bootstrapAskDbEnv, getAskDbRuntimeConfig } from "@askdb/config";
import { createAiRegistry } from "@askdb/ai";
import { openaiProvider } from "@askdb/ai-openai";
import { Pool } from "pg";

// Resolve schema, model, and dialect from askdb.config.ts — same config the CLI and Studio use.
bootstrapAskDbEnv({ cwd: process.cwd() });
const askdb = createAskDb({
  config: getAskDbRuntimeConfig(),
  registry: createAiRegistry([openaiProvider]),
  schema: { path: "./my-app.schema" }, // or set host.schemaPath in config and omit
});
const pool = new Pool({ connectionString: process.env.DATABASE_URL });

const { sql } = await askdb.ask("Which customers signed up last week?");

// Log or approve `sql` here, then run it through your own pool.
const result = await pool.query(sql);
```

  </TabItem>
</Tabs>

## Works with the database you already have

<HomeEngines />

## What the model sees — and what it doesn't

<HomePrivacyBoundary />

## Read next

<div class="home-path-grid">
  <a href="/quickstart/"><strong>Quickstart</strong><span>Install, introspect, and ask your first question in five minutes.</span></a>
  <a href="/concepts/how-askdb-works/"><strong>How AskDB works</strong><span>The end-to-end flow from question to validated SQL.</span></a>
  <a href="/guides/embed-in-node/"><strong>Embed in a Node app</strong><span>The full TypeScript walkthrough with a real connection pool.</span></a>
  <a href="/reference/packages/"><strong>Package reference</strong><span>Every <code>@askdb/*</code> package and what it does.</span></a>
</div>

</div>
