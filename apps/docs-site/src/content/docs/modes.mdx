---
title: "Modes and Safety"
description: "Operating modes define how much schema context reaches the model and how results may be summarized—the full specification lives with the source."
---

<p className="doc-eyebrow">Guardrails</p>

<p className="doc-lede">
  Modes fix <strong>trust boundaries</strong> for headless pipelines: what may enter model context beyond the AskDB schema and the natural-language question. The full <strong>modes</strong> specification is versioned alongside the code—see{" "}
  <a href="https://github.com/Ygilany/AskDB/blob/main/docs/contracts/modes-v1.md">modes contract (GitHub)</a>. Surfaces such as MCP (roadmap) and HTTP should reuse the same semantics—see <a href="../guides/agents-and-mcp/">Agents and MCP (status)</a>.
</p>

## SQL validation

<p>Generated SQL is expected to be a PostgreSQL read-only <code>{"SELECT"}</code>. Guardrails reject writes, multi-statements, and system-schema access.</p>

## schema_only

<p>The default mode. AskDB grounds generation in schema context and returns SQL only.</p>

```
askdb ask \
  --schema my-app.schema \
  --question "How many orders?" \
  --mode schema_only
```

## bounded_results

<p>Reserved for future workflows where a host application may summarize bounded tabular results. Current AskDB surfaces still return SQL only.</p>

```
askdb ask \
  --schema my-app.schema \
  --question "Show recent signups" \
  --mode bounded_results \
  -v
```

## Sensitive fields

<p>Sensitive column and table markers can be included in prompts as tagged names, or omitted with <code>{"--omit-sensitive-from-prompt"}</code> / <code>{"ASKDB_OMIT_SENSITIVE_FROM_PROMPT"}</code>. Full rules are in the{" "}
  <a href="https://github.com/Ygilany/AskDB/blob/main/docs/contracts/sensitive-fields-and-modes.md">sensitive fields and modes contract (GitHub)</a>.</p>

## When to read the full spec

<p>
  This page is a short operator view. When you are wiring automation or reviewing edge cases, open the <a href="https://github.com/Ygilany/AskDB/blob/main/docs/contracts/modes-v1.md">modes contract</a> alongside your integration code.
</p>
