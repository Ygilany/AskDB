---
title: "Modes and Safety"
description: "Operating modes define how much schema context reaches the model and how results may be summarized—see modes-v1 in the repo for the full contract."
---

<p className="doc-eyebrow">Guardrails</p>

<p className="doc-lede">
  Modes fix <strong>trust boundaries</strong> for headless pipelines: what may enter model context beyond the AskDB schema and the natural-language question. The authoritative contract is{" "}
  <a href="https://github.com/Ygilany/AskDB/blob/main/docs/contracts/modes-v1.md">docs/contracts/modes-v1.md</a>. Surfaces such as MCP (roadmap) and HTTP should reuse the same semantics—see <a href="../guides/agents-and-mcp/">Agents and MCP (status)</a>.
</p>

## SQL validation

<p>Generated SQL is expected to be a PostgreSQL read-only <code>{"SELECT"}</code>. Guardrails reject writes, multi-statements, and system-schema access.</p>

## schema_only

<p>The default mode. AskDB grounds generation in schema context and returns SQL only.</p>

```
askdb ask \
  --schema my-app.schema \
  --question "How many orders?" \
  --mode schema_only
```

## bounded_results

<p>Reserved for future workflows where a host application may summarize bounded tabular results. Current AskDB surfaces still return SQL only.</p>

```
askdb ask \
  --schema my-app.schema \
  --question "Show recent signups" \
  --mode bounded_results \
  -v
```

## Sensitive fields

<p>Sensitive column and table markers can be included in prompts as tagged names, or omitted with <code>{"--omit-sensitive-from-prompt"}</code> / <code>{"ASKDB_OMIT_SENSITIVE_FROM_PROMPT"}</code>. Full rules live in{" "}
  <a href="https://github.com/Ygilany/AskDB/blob/main/docs/contracts/sensitive-fields-and-modes.md">docs/contracts/sensitive-fields-and-modes.md</a>.</p>

## Spec in the repo

<p>
  This page is a short operator view. For merge-bar language and edge cases, read <a href="https://github.com/Ygilany/AskDB/blob/main/docs/contracts/modes-v1.md">modes-v1</a> next to your integration code.
</p>
