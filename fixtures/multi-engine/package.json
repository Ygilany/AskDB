{
  "name": "@askdb/fixture-multi-engine",
  "version": "0.0.0",
  "private": true,
  "description": "Shared multi-engine test fixture: one logical schema and dataset in PostgreSQL, MySQL, MariaDB, SQL Server and SQLite, for package integration tests and the consumer lab.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "up": "docker compose up -d --wait && pnpm run seed",
    "down": "docker compose down",
    "reset": "docker compose down -v && rm -rf .data && pnpm run up",
    "seed": "tsx src/seed.ts",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0",
    "mssql": "^12.5.5",
    "mysql2": "^3.22.5",
    "pg": "^8.21.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/mssql": "^12.3.0",
    "@types/node": "^22.20.1",
    "@types/pg": "^8.20.3",
    "tsx": "^4.23.5"
  }
}
