{
  "name": "askdb-consumer-lab",
  "version": "0.0.0",
  "private": true,
  "description": "Black-box consumer lab for AskDB: identical multi-dialect databases and a consumer app that installs AskDB like an outside developer would. Not a workspace member.",
  "type": "module",
  "engines": {
    "node": ">=22.12"
  },
  "packageManager": "pnpm@11.22.0",
  "scripts": {
    "up": "docker compose up -d --wait && pnpm run seed",
    "down": "docker compose down",
    "reset": "docker compose down -v && rm -rf .data && pnpm run up",
    "seed": "tsx src/seed.ts",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "better-sqlite3": "12.11.1",
    "mssql": "12.7.0",
    "mysql2": "3.23.2",
    "pg": "8.22.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "7.6.13",
    "@types/mssql": "12.3.0",
    "@types/node": "22.20.1",
    "@types/pg": "8.20.3",
    "tsx": "4.23.5",
    "typescript": "6.0.3",
    "vitest": "4.1.11"
  }
}
