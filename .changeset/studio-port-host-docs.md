---
---

docs(site): document Studio port and host configuration

- Note that Studio can change its default `127.0.0.1:5556` bind address with
  `--port`, `--host`, or the `studio.listen.*` config fields
- Add the missing `askdb studio --host <host>` row to the CLI reference
