---
"@askdb/studio": patch
---

Fix top-3 React Doctor issues: remove non-component exports from component files (fast refresh), replace double-pass map+filter with flatMap, and replace role="group" divs with semantic fieldset elements.
