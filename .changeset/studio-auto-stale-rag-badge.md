---
"@askdb/studio": patch
---

Fix RAG index badge not updating automatically after saving table enrichment, concepts, or tenant policy. The stale/fresh indicator on the nav bar and RAG page now refreshes immediately after any save without requiring a manual refresh click.
